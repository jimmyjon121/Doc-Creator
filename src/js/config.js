/**
 * CareConnect Pro - Config Module
 * Extracted from monolithic application
 */

(function(window, document) {
    'use strict';
    
// Configuration and Constants

const MASTER_USERNAME = 'MasterAdmin';

const MASTER_PASSWORD = 'FFA@dm1n2025!';

const LEGACY_USERNAME = 'Doc121';

const LEGACY_PASSWORD = 'FFA121';

const ACCOUNT_STORAGE_KEY = 'careconnect_user_accounts';


    
    // Export module
    window.CareConnect = window.CareConnect || {};
    window.CareConnect.config = {
        // Export public functions here
    };
    
})(window, document);
